# 第二类问题：观察解释与来源证据对应

## 改动

- readAnalysisObservations 在技法生成阶段按所请求的观察补充 grounding，包含宿主从冻结来源读取并定位的引文上下文。窗口为引文前后各最多 120 字，受原授权片段边界限制；校验绝对定位与引文一致。缺少旧定位时只在授权片段内定位，不能越界查找。
- quoted_text_outside_evidence 提取观察正文的中文引号文字，列出未被该观察 evidence 摘录覆盖的文字。它是字面核对提示，既不是语义错误结论，也不因未覆盖就删除观察。未识别所有引号形式，不声称覆盖所有归因错误。
- 工具明确 validationScope=literal_evidence_only，避免把字面引文匹配误当成解释正确。原始观察及其 contentDigest 保持不变，grounding 为单独的读取结果元数据。仅为模型实际读取的观察补充，不新增模型阶段；工具原有 48,000 字符响应上限保持，超出时要求减少读取 ID。
- 提取指引要求论断与引文对应，区分实际语言形式和推测效果，保留同一段内不同表达的配合。
- 核心生成提示词 v1.4：来源观察是待核对解释，采用前核对人物、顺序和语言形式；原文不支持时收窄或舍弃解释，不把局部特征变成排除其他写法的通用禁令。语义正确性仍由模型判断，代码不冒充文学判定器。
- 开发计划核心提示词及 fixtures 摘要同步；原测试材料和 judgeOnly 未修改。没有调整公开说明门禁、预算、推理配置或重试次数。

## 验证

相关 91 项回归通过，包含来源边界、错误定位拒绝、旧证据仅在授权范围查找、上下文大小限制、引用提示、工具读取冻结观察不变，以及分析流程和恢复。

真实复测继续使用已授权 E02 合成材料，隔离目录 `/tmp/purra-051-semantic-grounding-e02`，Kimi kimi-k2.6、enabled、16,384 单次生成预算。内容判断与运行状态、截断、工具门禁分别记录。

## 第一轮真实结果（v1.4）

Root `run_31e14d048adf4ef4` completed。提取 `run_c9dbb04ff70d4f31` 正确把屏住气归为追逐段落，也保留动作铺垫与明确点明羞愧的配合；仍错误地将“门还开着”归为省略主语。概览首次因引文无法继承来源范围被拒绝，原策略重试后成功；未放宽证据校验。技法生成读取了 9 条观察、11 个受限上下文和 4 处待核对引用提示，表明 grounding 已进入真实调用。

最终 SKILL.md 仍增加来源未规定的动作次数、句子字数限制，并保留省略主语的泛化写作建议。即便流程完成、没有截断，内容仍不通过。上下文补充不是语义正确性的保证，未据此宣称第二类问题全部解决。

## 补充修订与隔离验证（v1.5）

针对产物表现，核心指引进一步区分“可执行”与“量化规则”：不增补来源或用户没有规定的次数、字数等操作标准，不为每个观察凑操作要点；术语没有原句形式支持时，直接描述实际文字安排，不从未经支持的术语推导写作要求。

使用已有 evaluate-writing-techniques.py 在 `/tmp/purra-051-semantic-generation-e02` 单独运行 E02 生成，输入为预先核对的 O02a/O02b 标准观察及合成原文，不包含 judgeOnly；仍是已授权测试材料。这是生成阶段隔离检查，不能替代 v1.5 全流程或来源提取质量验证。

实现的全量检查通过（前端 462、后端 2252），最后一次提示词修订后相关 91 项回归再次通过。未运行额外全量重复检查来代替真实内容审查。

### v1.5 实际结果与验收边界

生成 Run `run_a4cc53fda8084744` done，5 次模型调用，generation 3,801、其中 reasoning 2,404。结构校验通过，产出单个 SKILL.md；未覆盖多文件。与 v1.4 的输入和阶段不同，不能用这两个数字比较整体成本或宣称性能改善。

人工查看文件后，本次没有出现 v1.4 中无依据的字数、次数限制或“门还开着”省略主语的错误；保留了长句、旧物反复、段末情绪，以及追逐的即时动作与感知。但仍将局部写法表述成“每个句子只容纳一个动作”等过强要求，因此整体内容不标记通过。输入本身要求两类场景分别选用，分开介绍有依据；“不要在同一段落中混合”仍比该任务选择要求更强。

独立使用中，追逐产物包含市场散场、即时行动与感知，未解释追逐来由；回忆产物有旧号码布与末尾表达再次参赛的愿望，但没有清楚承认“不甘心”，并将“一段”扩展成多段，不能将此任务标为完整通过。两个输出均为新上下文使用生成入口，不能证明来源提取质量。脚本 report 中的 structure=pass 与 contentReview=not_executed 保持原样；这里记录人工内容审查，不改写脚本状态为质量通过。

第二类问题已完成受限证据上下文接入与提示词修订，尚未完成内容质量验收。剩余问题明确为提取阶段的语法归类错误、生成阶段的过度规则化和独立使用的要求遗漏，不能由字面脚本校验推定修复。两次真实测试和回归进程均已退出；未启动开发服务，未重启用户后端，未修改真实小说。

## 后续修复：阶段输入与正式使用规则

v1.6 的完整尝试 `run_41bbc7748cb2425b` 中，语法误判仍出现，生成阶段因没有公开说明而在工具执行前被拒绝。另一轮 `run_6ce57200b9284298` 完成了流程，但观察继续附带使用建议，最终正文仍出现过强限制；这些尝试没有作为质量通过证据。

检查实际 Provider 输入后确认提取指令已送达，同时发现提取输入仍包含要求“编写技法、说明选用”的 analysisFocus。最终实现将该创作要求仅交给 distill_skill；提取、实体归一和概览只使用来源与各自契约。提取指引改为记录具体文字安排，不要求逐条补出效果、语法或修辞鉴定。完整链路回归检查取证阶段没有该字段、生成阶段仍收到原始要求。

核心生成提示词精简为 v1.7，要求提供可调整的创作建议，取消固定栏目暗示，避免为填取舍和适用条件而推导额外限制。入口、多文件、独立使用、材料不足与提交契约保留，计划和摘要同步。单元执行请求明确先履行现有公开说明约定再调用工具，未改变门禁和重试策略。

正式写作上下文与独立使用脚本共用 WRITING_TECHNIQUE_USE_POLICY，强调本轮内容、段落、情绪落点和结束位置优先。该策略即使自动模式尚无入口内容也必须提供，保证后续工具读取技法时生效；技法文件仍保持 untrusted。集成测试覆盖手动入口与自动稍后读取两种情况。

独立使用脚本原先固定 disabled / 4096，与完整测试的 enabled / 16384 不一致，现使用明确的 reasoning 参数并在报告中记录；这仅对齐验收条件，不是提高生产预算或修复内容错误的证明。工具对话保留 Provider 需要的 reasoning_content，仅用于同一调用链重放，不作为结果展示。

v1.7 标准观察生成 `run_d3196fe222894989` 的独立使用 U02a/U02b 在 enabled 模式下均符合一段要求：前者末尾明确承认并不甘心，后者围绕市场散场与即时追逐展开，没有解释追逐来由。该结果只支持本次独立写作测试；最终来源输入隔离后的完整验收另记。

## 确定性检查与冻结观察重放

完整 Run `run_a86c2dd21cda4cc8` 完成，实际输入证明提取没有 analysisFocus，生成仍保留原始要求。提取没有再次断言“门还开着”缺少主语谓语，保留明确点明羞愧的结尾，不再提前附加选用步骤；但生成把追逐原文直接用作“原创示例”，因此没有将该次完整产物作为质量通过。

增加 source_overlap：仅对生成工具待保存内容与本轮冻结观察证据进行连续 32 个字母/数字字符匹配，忽略标点和换行，不跨独立证据边界拼接。命中返回路径、行号和匹配片段，保存前拒绝且草稿代次不变。它不是语义判定、相似改写检测或完整抄袭检测；用户编辑与导入不受此生成工具检查影响。回归覆盖跨行复用、短句不误报、证据边界与失败后原创修订正常保存。

evaluate-writing-techniques.py 增加 --replay-analysis-dir，要求原报告来源摘要与所选 fixture 完全一致且只有一个冻结小节。原观察正文、标题和证据内容不变，只映射隔离库中的小节身份并重算摘要；报告保存来源 Run 与 Artifact 摘要。这样只复测生成，不重复提取或用标准观察替换模型观察。

重放生成 `run_1b384ef0dfd745a2` 完成，6 次调用，generation 4,167（其中 reasoning 2,188）。正文未出现连续原文复用、固定字数次数要求或禁止组合两种处理的通用要求。其省略主语例子改为“扶住墙”，不再用“门还开着”证明该语法结论。该轮没有触发复用拒绝，拒绝后的恢复由确定性集成测试覆盖，不声称已验证真实模型自动修复命中。

独立使用仍出现一次多段输出，证明共享提示词不能保证固定格式。新增 SingleProseParagraphValidator，通过正式 Writing response validators 接入现有响应修复；只保守识别明确请求一段场景/回忆等正文的任务，代码、多段、多个写作任务不启用。单段内部普通换行允许，空行分段返回定向修复要求；不校验情绪含义或擅自拼接正文。独立测试复用同一校验，保存被拒候选，要求同一上下文仅修正格式；不重跑来源分析或技法生成。

## 最终验证结果

- 来源提取输入隔离、生成 v1.7、原文复用检查与正式使用规则均已实现。使用 `/tmp/semantic-final-e02` 的真实观察重放生成，未改动其观察正文以通过测试；原始完整产物中发现的复制问题保留为失败证据。
- `/tmp/semantic-final-independent-use/independent-use.json` 使用重放产物与 enabled 配置完成两项检查。U02a 首次因多段被拒，第二次保留内容并整理为一段，末尾为“他到底还是不甘心”；U02b 一次完成，为一段市场散场中的躲避追赶描写，没有补写追逐原因。两者最终均以 stop 结束。格式修正不是再次提取或生成技法。
- 最终后端完整套件 2,268 项通过；前端 462 项、类型和边界检查已通过，后续仅增加后端及验收脚本修改。`--validate-only` 检查 8 份来源、9 个用例通过，fixture 的正文与内容判定标准未更改。`git diff --check` 通过。
- 内容核对通过范围为本次 E02 与两项独立写作要求；结构通过、字面检查和语义核对分别记录，不外推为所有来源、多文件或所有模型均已验收，也不从不同测试输入比较 token 性能。
- 重放生成退出时 httpcore2 曾记录异步迭代器关闭诊断；生成 Run 正常 done，进程退出码为 0，未将该诊断记为生成失败或内容修复证据。最后独立使用复测正常退出，无该关闭诊断。验证时未修改 PurrA 或已安装依赖源码。
