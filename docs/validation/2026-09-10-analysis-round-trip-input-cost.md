# 提取阶段往返次数与累计输入

## 真实运行定位

run_88dd8483409442fd 的根 Run 失败码为 max_model_rounds。子 Run run_5fc56383ed784c00 有 16 次 Provider 用量记录：累计输入 153309 token，单次 5700–13537 token，缓存输入累计 130048。另一个成功子 Run run_463ae82a629d45a6 有 14 次调用，累计输入 105578 token，单次 5712–10046 token，缓存输入累计 95104。累计输入不是同等大小的单次上下文，也不能直接推算费用。

失败子 Run 的第一轮消息内容约 6243 字符：任务规则、宿主上下文和约 2655 字符的单元输入。证据查找、空结果换词、修正字段和工具回执使多轮累计输入增加。本次查到 claimNature=unresolved、技法缺 evidence、合法 sourceSpanId 附带展示字段被拒绝等情况。

## 变更

提取输入中将 sourceEvidence.text 替换为带 sourceSpanId 的 excerpts；不再同时发送一份纯文本。编号由宿主按冻结来源和位置生成，模型直接选择已有编号提交，搜索仅补查。原始冻结 payload 不变。

补查无完整短语匹配时按空格关键词和标点归一返回精确原文候选，明确需要模型判断其支持关系，不使用模糊匹配自动认领引文。合法 sourceSpanId 优先，展示字段不作为实际证据写入；仍校验编号、版本和范围。

提示及工具说明明确 claimNature 与 lifecycleStatus 的区别、观察必须包含 evidence，以及合批保存、直接选择已有编号。保留原有运行轮数保护，没有以提高上限掩盖往返问题。

## 验证与边界

确定性测试覆盖输入只含一份带编号原文、原始 payload 不变、无需搜索即可解引用、混合展示字段处理、关键词候选，以及原有提交和来源保护。对话 Provider 模拟器改为读取新 excerpts 协议。

没有调用真实 Provider、重跑用户任务或重启后端。带编号原文会增加少量单次输入元数据，预期通过减少往返降低累计输入，实际下降幅度尚未验证。已经失败的 Run 不会被本次修改自动续跑。
