# 引文排版容错与批次部分保存

## 行为

引文定位只忽略空白字符，不忽略标点或改变文字。匹配始终限制在绑定的来源与片段内；归一后有多个匹配仍拒绝。成功后回填真实原文、原始字符位置和原文摘要，不保存模型改写的排版作为证据。

分批保存资料和观察时按条目验证所有证据。混合批次只持久化完整有效的条目，返回 partial、savedCount、rejected（原批次零基序号及错误）和仅重交失败项的提示。全部无效仍返回未发生写入的输入错误；权限错误不降级。沿用工具调用幂等与最终汇总去重。

## 证据

- test_analysis_evidence_references、test_writing_technique_generation、test_novel_analysis：95 项确定性测试通过。
- 覆盖原文位置及摘要回填、文字与标点变化拒绝、范围限制、排版归一后的歧义拒绝、混合批次保存与同调用幂等、最终汇总。
- 对 run_02c82c5cb0a649f0 所属任务已记录的 appendAnalysisFacts 参数进行只读回放：15 处旧匹配不通过的引文，7 处空白差异通过，8 处仍拒绝；通过项的位置均能反查出完全相同的真实原文。
- 未重跑真实 Provider，未修改真实资料与历史数据库，未进行 Electron 验收。已有后端进程需加载更新后才生效。

## 来源范围参数补充修复

针对 run_ed17eb96e27b409a 的 `novel analysis evidence range is incomplete`：分批保存先剔除模型填写的片段标识和起止位置，再由宿主绑定恢复。仍保留并校验 sectionId，不能借位置补全扩大来源范围。候选归一化改为逐条执行，其输入错误参与部分保存；全部无效时返回可修正的 tool_input_invalid，不再误归类为 invalid_reference。

补充测试覆盖遗漏结束位置、宿主位置回填、候选字段错误的部分保存、全部错误的分类与跨来源拒绝。相关三组测试共 96 项通过。未重启或重跑失败的真实任务，未调用 Provider，未改写真实数据。
