# 写作方法蒸馏真实验证记录

## 结论

2026-09-06（北京时间），使用已配置的 GLM-5.3-Flash 和一个 2,225 字符的来源章节，在临时数据库中验证。任务最终失败，错误为 `model_output_truncated`。没有生成最终写作方法；无法据此评价新方法的质量，也不能声称真实端到端验收通过。

## 已确认的执行结果

- 原始运行完成了冻结计划，但首个章节提取长时间持续生成而未提交结果，该次运行被中止。
- 本地模型能力声明只允许开启思考。关闭思考的尝试在本地能力校验处被拒绝，没有发出该模式的模型请求。
- 随后保留思考模式，将单次输出上限设为 10,000 Token，恢复同一临时任务并复用冻结计划。
- 两次受限的章节提取调用均消耗 10,000 个生成 Token，Provider 分别报告其中 9,972 和 9,982 个思考 Token，均未成功提交结果。这些数字不包含前置读取工具调用的用量，也不是整个验证过程的总费用。
- 重试额度耗尽后，提取单元失败，所有下游单元因依赖失败取消。归一、聚合、蒸馏、试写、修订和复核均未完成。
- 临时验证进程已正常退出。验证没有修改正式来源或书籍，也没有向正式方法库写入候选。

持续的 Provider 输出以及最终用量表明模型请求确实执行了。现有证据只能确认输出预算耗尽，尚不足以把原因归结为提示词、工具协议或模型自身的某一项缺陷。

## 复查位置

- 临时数据库：`/var/folders/c5/y2y2mkwd5n5ccrlt13qwjxhr0000gn/T/purrtypos-distill-live-jubg5glj/purrtypos.db`
- 任务：`longtask_d980627ea93942b9bc59f8b9417844d0`
- 两次受限提取运行：`run_e13d5bf4d661472c`、`run_bfd66122b1f942e0`
- 最终日志：`/tmp/purrtypos-distill-resume.log`

以上为本机临时证据，可能被系统清理。用量来自持久化 `provider.usage`，失败来自任务单元状态；记录不包含私有思考文本或凭据。

## 确定性验证

重写后的最终后端全套 2,069 项测试通过，前端 435 项测试通过；类型检查、Agent 架构检查与 Web 构建通过。浏览器检查覆盖方法展示和试写展开组件。模拟工具流验证了持久化、阶段依赖和失败阻断，但不代表真实模型成功生成了方法。

下一次真实验收必须先让相同章节稳定提交来源提取结果，再完成整个蒸馏流程，最后审查真实试写与证据。继续增加环节或仅提高输出上限不能作为质量已改善的证明。
