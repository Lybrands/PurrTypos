# 剧本 Run af73：耗时审计与诊断修复

审计对象：`run_af73273a51574672`。证据来自本地持久化 Run、事件、工具返回及 Revision，只读查询，不修改历史输入或输出。

## 真实 Run 的结论

- UTC 2026-09-04 20:50:24 至 21:11:36，总计 21 分 12 秒。
- Root 与 9 个子 Run 全部 `done`，26 次模型尝试。此样本没有再次发生输出截断。
- 输入 235,150 Token；生成 128,796 Token，其中 Provider 上报推理 118,205 Token。生成包括推理，不能将其全部当成可见正文。
- 本次实际生成上限来自模型能力：393,216 Token；没有用户配置上限，没有把 Part 的结果容量目标当作生成上限，也没有降低思考配置。因此此样本支持上一轮预算修复已生效，不构成所有模型、所有输出规模都已根治的证明。
- 已记录模型 Operation 累计 1,248,938 ms（20 分 48.938 秒），还不包含漏记 Operation 的私有 checkpoint 调用。不能将剩余时间直接作为宿主纯开销；可以确定绝大多数等待发生在模型调用内。
- 首次 Planner 的首个模型活动为 688 ms，首段公开进展为 73,753 ms。快速开始思考与快速产生可见文字不是同一件事。
- 初次计划因 `taskSpec.deliverable` 枚举混入中文解释而校验失败，约 75.7 秒后再进行约 3.1 秒修复。不能声称约 75 秒全部由修复调用导致。

## 修复范围

1. 诊断中的 Provider 调用、消息片段、模型操作、Planner 尝试以及工具输入/输出逐条默认收起。字符长度按 Unicode 码点显示；诊断序列化字符数不是 Provider Token 数。
2. 当前任务和执行段明确区分输入/输出。历史耗时改用持久化 Run 时间，修复回放几十毫秒被误当成实际执行耗时的问题。
3. Root 初建与续跑保存模型来源。历史缺失模型身份只从 invocation receipt 投影补全，不回写 Run。私有 checkpoint 模型任务接入标准 Operation 生命周期。
4. Provider 实际输入按 `stream.opened` invocation receipt 查询，覆盖没有 `model.call_recorded` 的私有模型任务。该 Root 原来显示 2 次，当前查询显示 3 次。未捕获的 Operation 不补造；开发诊断继续脱敏并在非开发环境关闭。
5. 具体工具名称从绑定场景顺序恢复，例如“为第 1 集读取第 4 场的场景计划、衔接与改编依据”。同名但不同场景不是重复执行。
6. 有持久化 Run 的执行段只保留一套工具调用列表；参数/结果嵌入其下，不再与“工具执行”重复列出。
7. 项目文档类型改为侧边导航，顺序固定为原作分析、创作简报、结构设计、场景规划、剧本正文、审阅修订。版本选择仍保留，未保存编辑的切换保护不变。

## 编排优化与边界

- 场景读取仍显式调用工具，不恢复自动材料注入。场景计划、前场衔接和创作简报保留；原作分析由全量正文改成带长度的章节目录，再按 `sectionKeys` 读取所需部分。目录在正文大小裁剪之前生成，读取仍锁定任务参考 Revision。
- 对此样本的同一份历史场景工具返回做离线重算，7 场材料从每场 27,243～28,982 字符降到 11,796～13,535 字符，每场减少 15,447 字符；创作简报不变。这是材料大小对照，不是新 Provider 实测 Token 或耗时改善。
- 集元信息 Part 直接依赖本集全部场景，预先给出可读取键；读取场景正文而不是只给 1,200 字的结尾。返回超过既有预算时明确标记裁剪，不能当成完整正文。减少先列目录、再补读其余场景的发现往返。
- Planner 明确枚举字段只允许枚举值，解释放入标题/目标。仍严格校验，不吞错、不自动篡改模型输出。
- 保留场景前后依赖及模型思考配置。此改动没有证明七场串行创作已经能在目标时间内完成；按需读取可能新增必要调用，应以新 Run 总耗时和质量一起验收。
- Recipe 升至 9，旧 Recipe 的未完成任务不能原地续跑，应重新发起；已完成 Run 与 Revision 不删除、不迁改。

## 验证

- 针对性回归覆盖目录在大文档上的完整性、章节读取、Revision 锁定、私有 Operation、历史身份只读补全、脱敏、完整场景依赖、折叠状态、字符数与侧栏顺序。
- 使用真实 SQLite 数据经只读适配器验证当前代码：Root 恢复 3 条输入；七场工具标签均有正确序号；实际 Revision 章节选择通过；ASGI 开发接口返回 3 条输入，关闭开发诊断后拒绝访问。没有真实 Provider 调用。
- 浏览器确认侧边导航、切换选中态、逐项折叠、输入/输出计数及 21 分 12 秒的持久化耗时。
- `npm run check:agent-refactor` 通过：组件规范、TypeScript、前端 435 项、后端 2059 项；最后目录参数校验调整后，100 项针对性测试通过。最终 `npm run build:web` 与 `git diff --check` 通过。
- 已有开发后端尚未重启，浏览器对应后端的新查询/编排逻辑需重启后生效；离线和 ASGI 检查直接运行当前代码。本轮没有新启动常驻测试服务，原有 18321/5174 服务保持不变。
- 尚未发起修改后的付费创作 Run，不宣称首段公开进展或整集耗时已经下降。下一次真实验收必须记录相同模型/思考配置、实际输入、每次调用耗时、首活动/首公开进展与最终场景质量。
