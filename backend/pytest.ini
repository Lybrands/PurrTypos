[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    real_provider: calls a paid external model provider and is a release gate
