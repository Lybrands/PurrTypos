"""Canonical home for the replacement Agent implementations.

The existing Agent code remains in its historical modules while it is frozen.
New implementation code must be added below this package instead of extending
the legacy modules.
"""
